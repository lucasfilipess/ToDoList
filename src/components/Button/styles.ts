import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  button: {
    backgroundColor: 'coral',
    padding: 16,
    borderRadius: 8,
  },
  disabled: {
    opacity: 0.7,
  },
  label: {
    textAlign: 'center',
    color: 'white',
  },
});
