import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textInput: {
    height: 50,
    paddingHorizontal: 16,
    borderWidth: 1,
    borderColor: 'gray',
    borderStyle: 'solid',
    borderRadius: 4,
    color: 'coral',
  },
});
